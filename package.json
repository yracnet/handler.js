{
  "name": "handler",
  "description": "Handler JS for common navegation",
  "version": "1.0.0",
  "author": "Willyams Yujra <yracnet@gmail.com>",
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^6.2.0",
    "rollup": "^1.19.4"
  },
  "scripts": {
    "clean": "reset && rm -rf target",
    "build": "reset && ./node_modules/.bin/rollup -c",
    "test": "reset && ./node_modules/.bin/mocha 'src/test/**/*Test.js'"
  },
  "license": "MIT"
}
